{{> search-section}}

<div class="books-grid">
    {{#each books}}
    {{> book-card this basePath=../basePath isAuthors=../isAuthors isCategories=../isCategories}}
    {{/each}}
</div>

<div class="stats">
    <p>Showing <span id="filteredCount">{{books.length}}</span> of {{totalBooks}} books</p>
</div>
